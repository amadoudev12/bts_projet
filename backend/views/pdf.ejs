<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bulletin</title>

    <style>
        body {
            font-family: "Segoe UI", Tahoma, sans-serif;
            background: #f5f5f5;
            padding: 40px;
        }

        .container {
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0px 4px 20px rgba(0,0,0,0.1);
            max-width: 900px;
            margin: auto;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #2b2b2b;
        }

        .header-info {
            margin-bottom: 25px;
            font-size: 16px;
            line-height: 1.6;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            font-size: 15px;
        }

        thead {
            background: #4a4a4a;
            color: white;
        }

        th, td {
            padding: 12px;
            border: 1px solid #ddd;
            text-align: center;
        }

        tbody tr:nth-child(even) {
            background: #f8f8f8;
        }

        tbody tr:hover {
            background: #eaeaea;
        }

        .footer {
            margin-top: 30px;
            font-size: 18px;
            text-align: right;
        }

        .footer span {
            font-weight: bold;
        }

        .signature-zone {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
        }

        .signature-block {
            width: 40%;
            text-align: center;
        }

        .signature-block .line {
            margin-top: 60px;
            border-top: 1px solid #333;
            width: 100%;
        }

    </style>
</head>
<body>

<div class="container">

    <h1>🔹 Bulletin de Notes 🔹</h1>

    <div class="header-info">
        <strong>Matricule :</strong> <%= etudiant.id_etudiant %> <br>
        <strong>Nom de l'étudiant :</strong> <%= etudiant.nom %> <%= etudiant?.prenom || "Nom étudiant" %> <br>
        <strong>Date de naissance :</strong> <%= etudiant.date_naissance %>  <br>
        <strong>Classe :</strong> <%= etudiant.nom_filiere || "Classe" %> <br>
        <strong>Année académique :</strong> 2024 – 2025
    </div> 

    <table>
        <thead>
            <tr>
                <th>Matière</th>
                <th>Note</th>
                <th>Coefficient</th>
                <th>Cumul</th>
            </tr>
        </thead>

        <tbody>
            <% notes.forEach(note => { %>
                <tr>
                    <td><%= note.nom_matiere %></td>
                    <td><%= note.note %></td>
                    <td><%= note.coefficient %></td>
                    <td><%= note.coefficient * note.note %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <div class="footer">
        Total : <span><%= total %></span> <br>
        Moyenne : <span><%= moyenne %></span>
    </div>

    <div class="signature-zone">
        <div class="signature-block">
            <strong>Signature du Professeur</strong>
            <div class="line"></div>
        </div>

        <div class="signature-block">
            <strong>Signature de l'Établissement</strong>
            <div class="line"></div>
        </div>
    </div>

</div>

</body>
</html>